2026-08-20 — Dedicated Full-Screen Login Page & Isolated Auth Layout

What changed:
- Isolated layout in root layout (src/app/layout.tsx):
  * When navigating to /login, the application bypasses the Sidebar, Header, CommandPalette, and QuickCaptureModal to present a clean, dedicated full-screen authentication interface.
  * Preserved full dashboard sidebar layout for all internal authenticated pages.
- Enhanced login page experience (src/app/login/page.tsx):
  * Added 1-click developer credential autofill button for instant local demo access.
  * Added interactive password visibility toggle (Eye / EyeOff).
  * Added cyber/dark gradient accents and ambient glow effects.
  * Added clear notices and security badges for Neon Serverless Postgres and JWT Edge auth.
- Pushed updates directly to GitHub repository origin main.

Why:
- To provide a dedicated, polished full-screen login interface without layout clutter when unauthenticated users access the platform.

Files affected:
- src/app/layout.tsx — Layout branching for auth vs application views
- src/app/login/page.tsx — Dedicated login page with autofill and visibility toggle
- commit.txt — Updated tracking record

Breaking changes:
- None.

Notes for review:
- Default credentials remain:
  * Email: admin@knowledge.local
  * Password: AdminPassword123!
