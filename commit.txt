2026-08-20 — Professional Linear & Raycast UI/UX Redesign

What changed:
- Upgraded Design System to Linear/Geist standards (src/app/globals.css):
  * Added obsidian background palette (#070a12) with radial luminous lighting layers.
  * Introduced glassmorphic surface utility classes (.glass-panel, .glass-card) with fine borders (border-white/[0.07]) and subtle hover elevation.
  * Added custom sleek scrollbars and micro-interaction focus rings.
- Refactored Navigation & Layout:
  * Header (src/components/layout/Header.tsx): Raycast/Linear style search trigger with keyboard badges (⌘K), dynamic breadcrumbs, and gradient action buttons.
  * Sidebar (src/components/layout/Sidebar.tsx): Linear workspace branding, active tab glowing indicators, categorized count badges, and user session footer.
- Redesigned Key Views:
  * Dashboard (src/app/page.tsx): Spotlight search hero with technical suggestion chips, minimal KPI metric cards with icons and trend badges, and glassmorphic leaderboard containers.
  * Incident Explorer (src/app/entries/page.tsx): Refined multi-criteria filter bar, segmented status selectors, glass cards with quality score rings, and 1-click command copying.
  * Incident Detail & Runbook (src/app/entries/[id]/page.tsx): Prominent Quick-Fix hero banner, glowing timeline spine for investigation hypotheses, terminal code containers, and tabbed runbook views.
  * Command Library (src/app/commands/page.tsx): Raycast command cheat sheet layout with language filters and copy feedback.
  * Quality Score Badge (src/components/entries/QualityBadge.tsx): Micro-progress bar and glassmorphic checklist popover breakdown.
- Synchronized and pushed updates to GitHub repository origin main.

Why:
- To align the user interface with industry-leading developer tools (Linear, Raycast, Supabase, Vercel) with high-density information, refined contrast, and clean micro-interactions.

Files affected:
- src/app/globals.css — Design tokens, glassmorphism, and radial gradients
- src/components/layout/Header.tsx — Raycast search trigger and breadcrumbs
- src/components/layout/Sidebar.tsx — Linear navigation and workspace header
- src/app/page.tsx — Dashboard spotlight and KPI redesign
- src/app/entries/page.tsx — Explorer filters and glass card redesign
- src/app/entries/[id]/page.tsx — Incident sheet, tabs, and runbook layout
- src/components/entries/QuickFixCard.tsx — Quick-Fix hero banner and terminal cards
- src/components/entries/InvestigationTimeline.tsx — Glowing investigation spine
- src/components/entries/QualityBadge.tsx — Micro progress bar and popover
- src/app/commands/page.tsx — Command library redesign
- commit.txt — Updated tracking record

Breaking changes:
- None.
